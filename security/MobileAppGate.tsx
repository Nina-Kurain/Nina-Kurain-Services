"use client";

/**
 * Mobile App Gate (Disabled per security policy preferences).
 * Mobile users can browse directly without being blocked by an app-install dialog.
 */
export function MobileAppGate() {
  return null;
}

export default MobileAppGate;
